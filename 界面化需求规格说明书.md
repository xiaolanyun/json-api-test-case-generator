# JSON 接口测试数据生成器——界面化需求规格说明书

## 1. 文档目的

将当前命令行工具界面化，降低测试人员编写 JSON 配置文件、执行命令和查找产物的成本；用户通过浏览器界面完成字段分析、必填字段配置、用例生成、预览和下载。

本需求以现有 `json_case_generator.py` 的已实现能力为基线：解析 JSON、识别嵌套对象和数组对象、处理重名字段、生成必填字段的缺失/为空/类型错误测试数据，以及包含 `request_body` 的 CSV。

## 2. 产品定位与范围

### 2.1 产品定位

面向接口测试人员的本地 Web 工具。输入一份合法 JSON 请求体样例，配置哪些字段为必填项和预期状态码，快速得到可导入 Postman 或 Apifox 的 CSV 测试数据。

### 2.2 目标用户

- 接口测试人员：根据接口请求样例生成参数校验测试数据。
- 开发人员：自测接口参数校验的边界场景。
- 测试负责人：统一团队生成用例数据的方式和命名。

### 2.3 一期范围（MVP）

- 仅支持单用户、本机运行、浏览器访问，不需要登录、权限、数据库或服务器部署。
- 仅支持 JSON 请求体样例输入。
- 支持字段分析、必填字段配置、生成、预览和下载 CSV。
- 保持当前三类异常用例语义：字段缺失、内容为空、字段类型错误。
- 保持 CSV 适配 Postman Collection Runner 与 Apifox Run；`request_body` 为最终请求体。

### 2.4 明确不纳入一期

- 发送真实 HTTP 请求、鉴权配置、断言执行、接口测试报告。
- OpenAPI/Swagger 导入、Excel 导入、批量接口管理。
- 用户账户、多人协作、云端存储、历史项目服务端同步。
- 自定义任意复杂的测试策略（例如长度、枚举、边界值、正则、组合覆盖）。

这些能力应在当前“数据生成正确且易用”稳定后，作为后续迭代单独评估，避免界面版变成新的接口测试平台。

## 3. 核心业务流程

```text
输入 JSON（粘贴 / 上传）
        ↓
校验并分析字段
        ↓
展示字段树，用户勾选必填字段
        ↓
设置用例参数（状态码、是否校验容器字段）
        ↓
生成测试用例
        ↓
预览统计与 CSV 内容 → 下载单类 CSV 或全部 ZIP
```

## 4. 页面与功能需求

### 4.1 页面结构

一期采用单页三步向导，未完成前一步时后一步不可操作：

```text
+------------------------------------------------------------------+
| JSON 接口测试数据生成器                          [使用说明]       |
+------------------------------------------------------------------+
| 1 输入样例  ─────  2 配置字段  ─────  3 生成与下载                |
+------------------------------------------------------------------+
| 左侧：当前步骤的编辑区           | 右侧：JSON/请求体预览或帮助区   |
+------------------------------------------------------------------+
```

### 4.2 步骤一：输入与字段分析

| 编号 | 需求 | 验收标准 |
| --- | --- | --- |
| F-01 | 提供 JSON 文本编辑区，支持直接粘贴或手工编辑。 | 用户粘贴合法 JSON 后可分析。 |
| F-02 | 提供 `.json` 文件上传，上传内容填入编辑区。 | UTF-8 JSON 文件可正常读取；文件内容覆盖编辑区前应二次确认。 |
| F-03 | 提供“格式化”和“清空”操作。 | 格式化不改变 JSON 语义；清空需确认。 |
| F-04 | 点击“分析字段”后校验 JSON 并提取字段。 | 对合法 JSON 显示字段数和字段列表；非法 JSON 显示行、列和错误原因，不进入下一步。 |
| F-05 | 支持对象嵌套、数组中的对象、基本类型数组和空数组。 | 输出字段路径与当前脚本规则一致，例如 `items[].name`。 |
| F-06 | 自动处理同名字段。 | 同名字段使用稳定且可区分的变量名，例如 `name1`、`name2`，并显示原字段名和完整路径。 |

输入限制：一期建议单份 JSON 最大 1 MB 或 5,000 个字段；超出时提示用户拆分样例，不应导致页面失去响应。

### 4.3 步骤二：字段与生成配置

字段区域必须以“可搜索的表格 + 可展开字段树”呈现，默认显示表格；字段树用于理解层级，表格用于批量配置。

字段表列：必填、变量名、原字段名、完整路径、数据类型、父字段、是否容器、样例值。

| 编号 | 需求 | 验收标准 |
| --- | --- | --- |
| F-07 | 每个字段有“必填”复选框。 | 选中字段会生成对应异常用例；未选中字段不生成异常用例。 |
| F-08 | 支持关键词按变量名、原字段名或路径过滤。 | 输入关键词后仅显示匹配行，勾选状态不丢失。 |
| F-09 | 提供“全选可测字段”“清空选择”“仅选叶子字段”批量操作。 | 操作只改变当前可见/适用的字段并给出已选数量。 |
| F-10 | 容器字段（object、array 等）可单独勾选。 | 关闭“校验容器字段”时，容器字段保持可见但不可勾选，并说明原因。 |
| F-11 | 可设置正常用例预期状态码，默认 `200`；异常用例预期状态码，默认 `400`。 | 只接受 100–599 的整数；输入错误时不可生成。 |
| F-12 | 提供“校验容器字段”开关，默认开启。 | 开启时容器必填字段生成缺失、为空、类型错误用例；关闭时容器字段不生成上述用例。 |
| F-13 | 在配置完成后显示预计生成数量。 | 数量与最终生成行数一致：每个分类 CSV 都含 1 条正常基准用例；汇总 CSV 仅含 1 条正常基准用例。 |

### 4.4 步骤三：生成、预览和下载

| 编号 | 需求 | 验收标准 |
| --- | --- | --- |
| F-14 | 点击“生成测试用例”后，调用现有生成逻辑或等价服务层逻辑。 | 不改变当前字段解析和用例构造规则。 |
| F-15 | 显示生成汇总：字段总数、必填字段数、各类用例数量、总用例数。 | 数量与生成文件一致。 |
| F-16 | 按标签页预览四份结果：全部用例、缺失、为空、类型错误。 | “全部用例”固定排在首位，且每次生成后默认展示；默认显示前 100 行，显示总行数，支持横向查看全部字段列。 |
| F-17 | 每行支持展开/查看格式化后的 `request_body`。 | 缺失、空值、类型错误在请求体中的效果与当前命令行输出一致。 |
| F-18 | JSON 查看弹窗提供“一键复制”。 | 复制格式化后的完整 `request_body`；复制成功有明确提示，浏览器剪贴板权限受限时自动使用兼容复制方式。 |
| F-19 | 支持下载四份独立 CSV。 | 文件为 UTF-8 with BOM，Excel 打开中文不乱码；列顺序与预览一致。 |
| F-20 | 支持“一键下载全部”ZIP。 | ZIP 内含四份 CSV 和本次生成配置 JSON。 |
| F-21 | 支持返回第二步修改配置并重新生成。 | 已输入 JSON 与必填勾选、状态码配置均保留。 |

## 5. 生成规则（必须与现有工具保持一致）

### 5.1 基准与分类文件

每一次生成必须产生以下四类文件：

| 文件 | 内容 |
| --- | --- |
| `case_required_missing.csv` | 1 条正常基准用例 + 每个必填字段 1 条字段缺失用例。 |
| `case_required_empty.csv` | 1 条正常基准用例 + 每个必填字段 1 条内容为空用例。 |
| `case_required_type.csv` | 1 条正常基准用例 + 每个必填字段 1 条类型错误用例。 |
| `case_all.csv` | 1 条正常基准用例 + 上述三类异常用例去重后的汇总。 |

CSV 固定列顺序：`req_name`、全部字段变量列、`expected_code`、`test_purpose`、`case_type`、`request_body`。

### 5.2 用例规则

- 正常基准用例：使用输入样例原始值，`case_type=positive`。
- 必填字段缺失：从最终 `request_body` 删除目标字段；若目标为容器，其所有子字段也不适用。
- 必填字段为空：字符串变为 `""`，对象变为 `{}`，数组变为 `[]`；容器子字段不适用。
- 类型错误：为目标字段写入与其原类型不兼容的典型值。类型映射沿用当前脚本实现。
- `request_body` 是最终可直接发送的 JSON 请求体，是 Postman/Apifox 执行时的推荐数据源。
- CSV 中的 `DELETE_ME`、`EMPTY_OBJ`、`EMPTY_ARR`、`N/A`、`TYPE_ERROR` 是界面预览中可解释的内部标记；下载 CSV 时与现有规则保持一致。

## 6. 交互与易用性要求

- 任何不可执行操作须说明原因，例如“尚未分析合法 JSON”“未选择必填字段”。
- 生成前如果未勾选必填字段，允许生成，但明确提示“仅生成正常基准用例”。
- 字段超过 100 条时，表格采用分页或虚拟滚动；搜索、勾选和切换开关应在 300 ms 内响应。
- 页面应提供简短术语说明：容器字段、变量名、字段缺失、内容为空、类型错误、`request_body`。
- 所有提示、列名、下载文件名使用中文；保留 CSV 的既有英文技术列名，避免破坏 Postman/Apifox 使用方式。
- 输入 JSON 和当前配置仅保存在浏览器内存或本机临时目录；关闭页面后可不保留。一期不得上传到外网。

## 7. 异常处理

| 场景 | 系统行为 |
| --- | --- |
| JSON 语法错误 | 阻止分析，定位到行列并显示错误信息。 |
| 顶层输入不是对象或数组 | 可分析；在结果区显示顶层类型提示。 |
| 空 JSON 对象/数组 | 可分析，但提示“未发现可配置字段”，不能生成异常字段用例。 |
| 变量名冲突或路径异常 | 阻止生成并显示涉及字段与路径；不产生部分文件。 |
| 生成过程异常 | 保留当前输入和配置，展示可读错误信息及“重试”按钮。 |
| 下载失败 | 不丢失预览结果，可再次下载。 |

## 8. 非功能需求

| 类别 | 要求 |
| --- | --- |
| 运行方式 | 本机启动后用浏览器访问；默认仅监听 `127.0.0.1`。 |
| 安全与隐私 | 不上传 JSON、无外部网络依赖、不记录请求样例内容到日志。 |
| 兼容性 | 支持 Windows 10/11；Chrome、Edge 最近两个大版本。 |
| 性能 | 1 MB JSON 的分析与生成目标均不超过 3 秒；超限需明确提示。 |
| 可维护性 | 将页面、接口层、字段分析/用例生成核心逻辑分离；现有命令行能力可继续独立运行。 |
| 可测试性 | 核心生成逻辑须有单元测试；界面至少覆盖合法 JSON、非法 JSON、嵌套对象、数组对象、重名字段、容器字段开关和四类下载。 |

## 9. 建议的验收场景

1. 输入当前项目的 `input.json`，选择 `rclass` 与 `key` 为必填，正常码 200、异常码 400；汇总 CSV 应含 7 行（1 正常 + 2 字段 × 3 异常）。
2. 输入含嵌套对象和 `items: [{"name":"a"}]` 的 JSON；字段表应显示对象层级及 `items[].name` 路径。
3. 输入两个不同路径的 `name` 字段；变量名应区分为 `name1`、`name2`，且两者的缺失用例只删除各自目标字段。
4. 勾选对象字段并开启容器校验；应生成对象缺失、`{}` 空值和类型错误三个场景。关闭开关后，该对象不应产生异常用例。
5. 输入错误 JSON，例如缺少逗号；系统应指出错误位置，且不能进入字段配置步骤。
6. 生成后默认打开“全部用例”标签，且该标签位于所有分类标签首位；下载 `case_all.csv` 后，用 Excel 打开中文不乱码。
7. 在任一用例的 JSON 查看弹窗点击“一键复制”；应复制格式化后的 `request_body`，粘贴到 JSON 校验工具后可通过语法校验。

## 10. 迭代优先级与决策建议

### P0：必须完成后才能替代命令行

- JSON 输入、校验、字段分析。
- 必填字段勾选、容器字段开关、两个状态码配置。
- 三类异常用例生成、`request_body` 构造、“全部用例”默认预览、JSON 一键复制和四份 CSV 下载。
- JSON 错误提示、生成失败提示、中文 UTF-8 BOM CSV。

### P1：建议在 P0 稳定后补充

- 本地保存/恢复最近一次输入和配置。
- 生成配置导入/导出。
- 字段树批量勾选、结果表筛选。
- 下载 ZIP、深色模式和更完整的使用帮助。

### P2：只有确认真实使用频率后再立项

- OpenAPI/Swagger 自动读取请求样例。
- 枚举、长度、数值边界、格式校验等更多异常策略。
- 多接口批量生成与项目管理。
- Postman/Apifox API 集成、实际请求执行与测试报告。

## 11. 实施边界建议

优先采用轻量本地 Web 架构：Python 后端复用并整理现有生成核心，浏览器前端负责输入、配置、预览和下载。现有命令行脚本不应直接由界面拼接 shell 命令调用；应抽出可被命令行和 Web 路由共同调用的 Python 服务函数，以减少路径、编码和错误处理差异。

在开始开发前，产品侧只需最终确认两件事：一期是否坚持“本机单用户、不执行真实接口”的边界，以及下载“全部结果”是否默认采用 ZIP。其余行为可按本说明直接实现。
